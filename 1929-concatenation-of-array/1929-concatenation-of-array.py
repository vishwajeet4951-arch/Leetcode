class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        ans=[]
        arr1=[]
        arr2=[]
        for num in nums:
            ans.append(num)
        for num in nums:
            ans.append(num)
        return ans
        